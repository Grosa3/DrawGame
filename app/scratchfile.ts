
// A statement
let foo = 3;
// Calling a method or a function
ctx.moveTo(10, 10);

let somethingIsTrue = true;
function runSomeCommand() {

}

// Definine a function
function maybeAName(x, y) { // start block
  if (somethingIsTrue) { // start an if block
    runSomeCommand();
  } // end an if block
} // end block

let button = document.createElement('button');

button.addEventListener(
  'click',
  function (event) {   // star block
    runSomeCommand();
    //
    //
    //
    
  } // end blcok
); // end addEventListener